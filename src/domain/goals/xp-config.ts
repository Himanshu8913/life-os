import { XP_REWARDS } from '@/domain/progression/xp-config'

export { XP_REWARDS } from '@/domain/progression/xp-config'

/** XP awarded when a goal is fully completed (PLAN §38). */
export const GOAL_COMPLETION_XP = XP_REWARDS.goal

/** XP awarded per milestone completion. */
export const MILESTONE_COMPLETION_XP = XP_REWARDS.milestone
