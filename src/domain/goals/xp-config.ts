/** XP awarded when a goal is fully completed (PLAN §38). */
export const GOAL_COMPLETION_XP = 500

/** XP awarded per milestone completion (smaller reward). */
export const MILESTONE_COMPLETION_XP = 50
